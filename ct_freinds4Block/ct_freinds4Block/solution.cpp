#include <string>
#include <vector>

using namespace std;

// 지워질 블록 찾아서 지우기, 설마 재귀인가
// 8방향으로 검사해야함

// 보드를 세로로 읽을 수 있게 변환
vector<string> convertBoard(int m, int n, vector<string> board)
{
    vector<string> cvBoard;

    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
        {
            
        }
    }
}

int solution(int m, int n, vector<string> board) {

    int answer = 0;


    return answer;

}